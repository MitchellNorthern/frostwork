export function getStore() {}

/*
const target = {
    message1: 'hello',
    message2: 'everyone'
}

const handler = {}

const thingsToDo = []

const proxy = new Proxy(target, {
    set: (target, prop, val, receiver) => {
        thingsToDo.push(() => {
            return {
                target,
                prop,
                val,
                receiver
            }
        })
        return true
    }
})

console.log(proxy.message1)
proxy.message1 = 'new string'
*/
